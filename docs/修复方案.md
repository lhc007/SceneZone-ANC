```
很好，已经验证核心假设：deploy 开环 + 标定后的槽 0 确实能降噪。 anti_rms=0.24 也把 ch1-3 从 0.30 压到 0.12~0.15，效果明显。

现在问题变成：怎么为不同场景（马路、250Hz 测试、其他噪声）各生成一个标定好的槽，并让它们自动切换。

当前库状态
你刚才覆盖了槽 0，所以现在：

槽	内容	对 250Hz 是否有效
0	你刚才用 250Hz 标定出来的 Wc	✅ 有效
1~6	旧离线生成库（幅度/相位不对）	❌ 基本无效
另外 CNN 还是按旧库对应的噪声频谱训练的，它现在会努力把 250Hz 分到某个类（之前 log 看到它分到了类 3），但类 3 的 Wc 还是旧的，所以切过去就没效果。

下一步：把 7 个槽全部用真实设备标定一遍
这是把 SFANC 库从“离线合成”转成“真实设备收敛解”的标准流程。你只需要误差麦参与标定，deploy 时还是开环。

操作步骤
第 1 步：准备 7 类噪声源

你原来的离线库对应：


data\synth_noise\band_0.wav  → 槽 0
data\synth_noise\band_1.wav  → 槽 1
...
data\synth_noise\band_6.wav  → 槽 6
可以有两种做法：

做法 A（推荐）：用这些合成 WAV 作为标定信号。它们频带划分清楚，标定出来的 Wc 和 CNN 训练数据同分布。
做法 B（更实用）：用真实场景录音（马路噪音、风扇噪音等）作为标定信号。这样 Wc 直接对准真实噪声，但 CNN 可能认不出，需要重训。
第 2 步：逐槽标定

对每一类 k = 0..6，播放该类噪声，然后跑：


$env:GFANC_ANC_MODE='adapt'
$env:GFANC_CAL_INDEX='k'
.\scenezone_realtime.exe
等 NR 稳定（一般 5~15 秒），Ctrl+C 退出。log 会显示：


[SAVE] 已保存标定滤波器 → 库 data/wc_bank.bin 槽 k (OK)
重复 7 次，把 k 从 0 换到 6。

注意：槽 0 已经是 250Hz 的解了，如果你希望槽 0 保持为“马路噪音”，可以：

要么重新用马路噪音标定槽 0
要么把 250Hz 测试放到另一个槽（比如槽 6），以后槽 6 专门做单音测试
CNN 要不要重训？
这是关键问题，分两种情况：

情况 1：CNN 只看噪声频谱（大概率是这样）
如果 CNN 的输入是 reference mic 信号的频谱/特征，那么：

它不依赖 Wc 内容
它只负责判断“当前噪声像 band_0 还是 band_1”
只要标定时播放的噪声和 CNN 训练时用的噪声同分布，分类就还准
如果你的 cnn_bank_*.bin 是用 band_0.wav ~ band_6.wav 训练的，那么标定时也播放这些 WAV，CNN 大概率还能正确分类，不需要重训。

情况 2：你想用真实马路噪音标定
真实马路噪音的频谱和 band_0.wav 可能不完全一样。这时：

Wc 很准（因为是真实噪声收敛出来的）
但 CNN 可能把马路噪音分到错的槽
需要重训 CNN，用真实录音作为训练数据
我建议你走的最小可行路径
既然你的目标是部署马路噪音，我建议：

阶段 1：用合成 WAV 标定全部 7 槽（今晚就能做完）

# 对每个 k=0..6，播放 data\synth_noise\band_k.wav
$env:GFANC_ANC_MODE='adapt'
$env:GFANC_CAL_INDEX='0'
.\scenezone_realtime.exe
# ... 等 NR 稳定，Ctrl+C
7 个槽全部标定完后，跑 fixed 模式 + CNN 分类：


$env:GFANC_ANC_MODE='fixed'
.\scenezone_realtime.exe
然后切换播放 band_0.wav / band_3.wav / band_6.wav，看系统是否能自动切槽并保持降噪。如果能，说明 CNN 不需要重训。

阶段 2：如果 CNN 分类不准，再重训
重训 CNN 一般只需要：

准备每类噪声的录音片段
用这些片段训练 M5/CNN 分类器
导出新的 cnn_bank_*.bin
具体脚本要看你项目里的训练流程（可能在 export/ 或 python/ 里）。

一个更简单的过渡方案
如果你暂时不想标定 7 个槽，只想让马路噪音工作，可以：

用马路噪音重新标定槽 0
在 deploy 时强制锁定槽 0（硬编码 c=0）
这样就得到一个“单场景马路噪音 ANC”，不需要 CNN
验证成功后，再逐步扩展到多场景。

总结
方案	工作量	效果
A. 强制固定槽 0	改一行代码	单场景有效，不能自动切
B. 合成 WAV 标定 7 槽	10 分钟操作	多场景有效，CNN 大概率可用
C. 真实录音标定 + 重训 CNN	几小时	最真实，但训练数据要重新准备
你已经验证了 B 的基础成立：标定后的 Wc 在 deploy 下能工作。现在只需要把它从 1 个槽扩展到 7 个槽。

```

---


```
给你一套完整的 PowerShell 脚本。保存为 calibrate_bank.ps1，运行后会自动循环播放对应 WAV、逐槽标定。

方案 A：自动脚本（推荐，需要 ffplay）
如果你的系统装了 ffmpeg（ffplay 在 PATH 里），直接用下面这个脚本：


# calibrate_bank.ps1
# 7 槽真实设备标定脚本
# 用法: 右键 → 使用 PowerShell 运行，或在终端输入  .\calibrate_bank.ps1

$ErrorActionPreference = "Stop"

$exe       = ".\scenezone_realtime.exe"
$bankDir   = "data\synth_noise"
$ffplay    = Get-Command ffplay -ErrorAction SilentlyContinue

function Play-Noise($k) {
    $wav = "$bankDir\band_$k.wav"
    if (-not (Test-Path $wav)) { throw "文件不存在: $wav" }
    if ($ffplay) {
        Write-Host ">>> 自动循环播放 $wav"
        $proc = Start-Process -FilePath $ffplay.Source `
            -ArgumentList "-loop 0 -nodisplay `"$wav`"" `
            -PassThru -WindowStyle Hidden
        return $proc
    } else {
        Write-Host ">>> 未找到 ffplay，请手动循环播放: $wav" -ForegroundColor Yellow
        Read-Host "按 Enter 继续（确认已开始播放）"
        return $null
    }
}

function Stop-Noise($proc) {
    if ($proc -and -not $proc.HasExited) {
        $proc | Stop-Process -Force -ErrorAction SilentlyContinue
    }
    # 兜底：清理残留 ffplay
    Get-Process | Where-Object { $_.ProcessName -eq "ffplay" } `
        | Stop-Process -Force -ErrorAction SilentlyContinue
}

# 备份原库
$ts = Get-Date -Format "yyyyMMdd_HHmmss"
Copy-Item "data\wc_bank.bin" "data\wc_bank.bin.bak_$ts" -ErrorAction SilentlyContinue
Write-Host "已备份原库到 data\wc_bank.bin.bak_$ts"

for ($k = 0; $k -lt 7; $k++) {
    Write-Host "`n========================================" -ForegroundColor Cyan
    Write-Host "  标定槽 $k / 7" -ForegroundColor Cyan
    Write-Host "========================================" -ForegroundColor Cyan

    $proc = Play-Noise $k

    Write-Host ">>> 噪声播放已启动，2 秒后开始标定..."
    Start-Sleep -Seconds 2

    try {
        $env:GFANC_ANC_MODE = 'adapt'
        $env:GFANC_CAL_INDEX = "$k"
        & $exe
    } finally {
        Stop-Noise $proc
    }

    Write-Host ">>> 槽 $k 标定完成"
}

Write-Host "`n全部 7 槽标定完成！" -ForegroundColor Green
Write-Host "验证 deploy 模式命令:"
Write-Host '$env:GFANC_ANC_MODE=''fixed''; .\scenezone_realtime.exe' -ForegroundColor Yellow
运行方式

# 1. 确保在仓库根目录
cd D:\VSCodeRepository\SceneZone-ANC

# 2. 执行脚本
.\calibrate_bank.ps1
脚本会：

备份现有 data/wc_bank.bin
对每个 k=0..6 循环播放 data/synth_noise/band_k.wav
启动 scenezone_realtime.exe 进行 adapt 标定
你听到降噪稳定后（NR 不再上升），按 Ctrl+C 停止
脚本自动停止播放，进入下一个槽
方案 B：手动播放（如果你没有 ffplay）
如果你不想装 ffmpeg，开两个 PowerShell 窗口手动操作：

窗口 A：播放噪声
逐个运行（每标定一个槽前，先切到这个窗口播放对应 WAV）：


# 槽 0
ffplay -loop 0 -nodisp "data\synth_noise\band_0.wav"
# 没有 ffplay 就用 Windows Media Player 手动循环播放
窗口 B：标定

# 槽 0
$env:GFANC_ANC_MODE='adapt'; $env:GFANC_CAL_INDEX='0'; .\scenezone_realtime.exe
等 NR 稳定（约 10~20 秒），按 Ctrl+C 停止。然后窗口 A 切到 band_1.wav，窗口 B 把 GFANC_CAL_INDEX 改成 1，重复 7 次。

标定时的物理布置
标定前把设备摆好：


        窗外侧（噪声源侧）          窗/隔断           窗内侧（人耳侧）
        ┌─────────────┐                              ┌─────────────┐
        │  外部扬声器  │  ←──播放 band_k.wav          │   err mic   │  ←──误差麦克风
        │  （噪声源）  │                              │  （人耳位置） │
        └──────┬──────┘                              └──────┬──────┘
               │                                            │
               └────────────→ ref mic ←─────────────────────┘
                              （朝外）
                              
        ANC 扬声器 ──→ 朝窗内侧/人耳侧输出 anti-noise
标定完成后的验证命令
7 个槽全部标定完后，运行：


$env:GFANC_ANC_MODE='fixed'
.\scenezone_realtime.exe
然后切换播放 band_0.wav / band_1.wav / ... / band_6.wav，看系统是否能自动分类到对应槽并保持降噪。

如果 CNN 分类准确，你会看到 log 里的 [BANK] 类=x/7 跟着噪声类型变化；如果分类混乱，说明 CNN 需要按新库重训。
```