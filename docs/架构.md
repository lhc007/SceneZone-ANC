```
他的滤波器类别怎么区分？合成数据，但"滤波器本身"是真实的。

分两层，别混：

① CNN 分类器 = 纯合成数据训练（README 原话）：

80,000 条 1s 噪声实例，全部是白噪声 → 随机带通滤波器生成的合成噪声
每类标签 = 一个频带范围（class k ↔ 频带 k）
CNN 架构是 2D CNN（Modified_ShufflenetV2(num_classes=7)），输入是 Mel 频谱图（1s 噪声 → MelSpectrogram → 64×32 灰度图），不是时域波形
用的是 ImageNet 预训练的 shufflenet_v2_x0_5 微调
② 7 条控制滤波器 = 真实测量（README 原话）：

"7 pre-trained control filters are obtained in the 4-channel ANC window... 7 broadband noises with different frequency ranges are used as primary noises to obtain the corresponding pre-trained control filters"

7 条滤波器是在真实 ANC 窗户硬件上，用 7 段不同频段的宽带噪声做主噪声，逐段离线/在线 FxLMS 收敛得到的
这些滤波器是真实的（经真实声学路径），但驱动它们的噪声是合成的
类 k ↔ 滤波器 k 通过"频带"对齐：第 k 类合成噪声的频带 = 第 k 条滤波器训练的噪声频带
对你项目的影响——一个关键差异

SFANC-Window 的 CNN 输入是 Mel 谱图（2D 图像 CNN），你的项目是时域 1D CNN（m5_scene）。

你的 train_real_bank_cnn.py 里 m5_scene(K=K) 是 1D 卷积，吃 16000 样本时域波形。而 SFANC-Window 是 2D ShuffleNetV2 吃 Mel 图。架构不同，但架构选择不是必须复制的——你的合成数据生成器（generate_synthetic_noise.py）已经复刻了他的数据生成方法（白噪过随机带通、按频带定类），这是核心。

你的数据生成 vs 他的——已经对齐的部分
环节	SFANC-Window	你的 generate_synthetic_noise.py
合成方法	白噪→随机带通，随机中心频率和带宽	✅ 白噪→butter 带通，类频带内随机子带
标签	按频带定类	✅ 按频带定类
训练规模	80,000 条	可调（--clips-per-class，默认 2000×4=8000）
每类滤波器	7 段真实宽带噪声收敛	✅ band_k.wav（类 k 全频带宽带）喂 --filters
一个你应该注意的点：他用的是随机中心频率和带宽的带通（跨频带随机），你的生成器是"在类频带内随机子带"（窄带更贴合类频带）。两种都能训练出按频带分类的 CNN，但为了更像原论文，可以考虑让子带也能跨类频带随机——不过这不是必须，你的方案类语义更清晰。

关于论文
论文标题对上了：《Real-time Implementation and Explainable AI Analysis of Delayless CNN-based Selective Fixed-filter Active Noise Control》(MSSP 2024)。核心贡献和你项目一致的：

Delayless：时延无关（无逐样本在线自适应，CNN 慢速选滤波器）
Selective fixed-filter：固定滤波器库 + 选择，即你的 SFANC 硬选
Markov 过程理论：证明"按噪声类别切换滤波器"的合理性
LayerCAM 可解释性：可视化 CNN 看的是噪声的哪个频段
论文的重点是"为什么这么分类合理 + 怎么解释"，数据/训练方法就是上面代码里那套——没有额外的东西。

需要我把这些发现写进项目的 docs（比如合成噪声生成器的设计依据文档），或者调整生成器更贴近原论文吗？


```